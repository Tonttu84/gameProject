# **************************************************************************** #
#                                                                              #
#                                                         :::      ::::::::    #
#    TODO.txt                                           :+:      :+:    :+:    #
#                                                     +:+ +:+         +:+      #
#    By: jrimpila <jrimpila@hive.fi>                +#+  +:+       +#+         #
#                                                 +#+#+#+#+#+   +#+            #
#    Created: 2025/08/28 16:45:50 by jrimpila          #+#    #+#              #
#    Updated: 2025/10/04 13:08:19 by jrimpila         ###   ########.fr        #
#                                                                              #
# **************************************************************************** #

Movement speed / retreat cost: currently all units move 1 hex per tick in any
direction. Retreating while in contact with an enemy should be slower or costlier
than advancing — historically backing away under pressure was dangerous and tiring.
Needs a speed/movement-points system before this is expressible. Consider adding a
per-unit movementSpeed field (default 1) so faster units and penalties can be layered
on top without reworking all movement logic at once.

Accuracy system — needs design review:
  Currently accuracy is a 0-100 percentage used by ranged units for two things:
    1. Aimed range: accuracy/10 hexes — within this distance the unit can attempt
       to target an individual rather than just a hex.
    2. Aimed shot success: getRandom(1,100) <= accuracy — human archer at 50 has
       50-50, a gaze/mind-blast at 99 almost always picks its target.
  Open questions:
    - Should accuracy also affect the deviation of the shot (Deviate currently uses
      dist/accuracy but with hex distances the integer division usually gives 0,
      so it does nothing useful at the moment)?
    - Should melee units have a meaningful accuracy stat, or is it only for ranged/special?
    - The default accuracy=10 on AUnit means any unit with a special() that does a
      targeted attack gets 10% aimed chance and 1-hex aimed range — is that a sensible
      default or should it be 0 (no aimed shot) until explicitly set?

Armor fatigue: heavier armor should increase fatigueCost in combat (and possibly MARCHFATIGUE for
marching). Currently all unit fatigue costs are flat — armor type should be a multiplier or addend
on fatigueCost so plate > chain > leather > none. Soldier already has +1 heavy armor penalty as a
start; generalize this across all unit/armor types when adding more variety.

Body parts / hit locations (2026-07, builds on the BodyPart/Hittable stubs in Human.hpp):
  - Hits resolve to a body part; armor becomes per-piece items covering specific parts,
    each with: protection value, encumbrance penalty, and a defence penalty.
  - Encumbrance penalty applies to combat and at 2x to spellcasting (mages are meant to
    be discouraged from armoring up). Enc penalty can never be negative; the defence
    penalty CAN be negative (i.e. helpful) — never on mundane gear, but magical items
    may actively aid defence.
  - Size-based hit-location bias: tall attackers (use size as the proxy) hit heads more
    easily; a short attacker striking a larger target hits lower parts more often —
    unless wielding a long weapon (reach shifts the reachable locations upward, same
    spirit as the existing mount/rider reach shift in MountedUnit::pickMountTarget).

Historical archer mechanics (not yet implemented):
  - Morale suppression from volleys: arrow hits should inflict a temporary morale penalty
    on all units in the target hex, not just kill points. Historically the psychological
    effect of a volley was often larger than the casualties.
  - Forced advance under fire: units taking arrow fire should have a chance to fail a
    discipline check and advance early, breaking formation (Agincourt-style — French knights
    charged through mud because they couldn't endure the arrow storm).
  - Skirmisher behaviour: archers should automatically try to maintain a minimum distance
    from approaching enemies and retreat when melee units close in, baiting charges then
    falling back.
  - Volley bonus for massed fire: multiple archers targeting the same hex should multiply
    the morale suppression effect. A lone archer is irritating; a hundred is terrifying.

Mage fireball (disabled pending rewrite): direct hit = 10 damage to unit in target hex;
splash hits neighboring hexes for partial damage only if sizeUsed in that hex >= 40;
Deviate() deviation radius also needs retuning for hex scale

Add a solid mechanism for units to be pushed back. I think it will add natural mechanism how outflanking can pushe
unit together

Near hit 

Add shield wall


pre battle buffs for mages?

targeting, value for casts, pick the highest value cast

damage types bludgeon good vs armor, edged good once past armor, piercing?




add large creatures

//when moving forward the targets have an option to try to dodge backwards if they pass a defence check, 
if they fail they take damage and are moved to the grids after the creature



First shot should test vs shield * 2 if distracted vs shield * 1, I need to figure out distraction mechanics 

impliment Battlemage can still attack and move while get_cast() is ongoing 


Formations

Units being pushed back

Space mattering, crowded units needing space to win  but needs to change logic 

change to square Formations

arrows distrurb the whole square?


Lisaa strateginen kartta
    -luo taistelukentta dynaamisesti
    -paranna grafiikat

Migrate battle state to a DB, saving every tick, so battle replay can scrub forward and
back instead of only playing forward live.
  → DONE on feature/db-replay (2026-07): campaign-server/ (Express+Mongoose, embedded
    persistent mongod), ./game dump-units catalog sync, per-tick Tick docs, ReplayView
    scrubber in the frontend.

Do the DB-migration work on a separate branch first — design the schema and per-tick
storage approach and get it reviewed before touching the live engine/server code.

Later: a scheduled/CI contract test that the C++ engine's dump-units output matches what
is actually stored in the campaign DB (not just at boot-sync time) — catches a server
running against a stale DB or an engine rebuilt without a server restart. A first version
exists as campaign-server/tests/engine.integration.test.js (runs only when ./game is
built); the CI/scheduled variant should compare against the LIVE server's DB instead of
a fresh sync.

Build a proper auth module (deferred — big task, do as its own branch/session):
  → DONE on feature/auth (2026-07): fullstackopen pattern in campaign-server/ sharing
    the existing gamedb (users collection; Battle.user ref was already in place).
    POST /api/users (bcryptjs hash), POST /api/login (JWT, 1h expiry, secret from
    config.SECRET env), global tokenExtractor + per-route userExtractor; POST
    /api/battles is the protected route and stamps battles with their user. Frontend:
    LoginForm (login/register), session in localStorage ('loggedGameUser'), Fight
    button gated on login; replays/catalog stay public. The old plan of C++-side
    jwt-cpp/bcrypt became obsolete when the Node BFF became the only API boundary.
  Later: protect more routes (per-user battle history endpoint), rate-limit
  /api/login, and set a real SECRET in any non-local deployment.

Hexside combat score: per-hexside score that elevation, hits, damage, kills, support
ranks etc. all add into. How it gets used is decided later — but it can at least be
used from day one to deal damage to a fortified hexside (siege/attrition of works).
Related-but-later: the score causing morale checks and forcing the target hex to
retreat — hard to balance, explicitly not a priority yet.

Windows-native build (deferred — currently WSL-only on Windows machines):
  The Makefile is Linux-only: it downloads a prebuilt Linux SFML .so, links with ELF
  -rpath, uses ASan/UBSan/LeakSanitizer (not available on MinGW), and shells out to
  /usr/share/fonts. A native Windows port needs: Windows SFML binaries (or static
  build), a sanitizer-free build profile (or clang-cl sanitizers), font-path handling,
  and replacing the POSIX bits (unistd.h, /tmp pipe files in the server's subprocess
  model). Meanwhile: develop/test via WSL, which works end-to-end.